import { AddBlanksPipe } from './add-blanks.pipe';

describe('AddBlanksPipe', () => {
  it('create an instance', () => {
    const pipe = new AddBlanksPipe();
    expect(pipe).toBeTruthy();
  });
});
